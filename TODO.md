# TODO

- [x] Update navbar (desktop + mobile): add "Our Classes" link to `#classes` after Services.
- [x] Update footer quick links: add "Our Classes" link to `#classes`.
- [x] Implement new `#classes` section in `index.html` using the 3 images from `images/classes/`.
- [ ] Add/adjust minimal CSS in `style.css` for the classes cards/grid to match existing UI.
- [ ] Run a quick sanity check (open in browser) to ensure anchors and layout work.

